For setting up Redux & Saga run the following commands inside react root directory (Note: src folder must be present)
```sh
$  curl -X GET 'http://192.168.29.188:3000/redux-saga?path=C:/users/arsal/downloads/testing'
```

For running Makefile run `make run` and to stop the container run `make stop`. This will automatically build and run the container
